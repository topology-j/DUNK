<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springproject.dunk.mapper.st.NoticeBoardMapper">
<select id ="noticeBoardList" resultType="NoticeBoard" resultMap="boardResultMap">
	SELECT
	  *
	FROM notice
	ORDER BY no DESC
</select>
<select id = "getNoticeBoard" resultType="noticeBoard" resultMap="boardResultMap">
	SELECT
	*
	FROM notice
	WHERE no = #{no}
</select>

	<resultMap id ="boardResultMap" type="NoticeBoard">
		<id property ="no" column="no"/>
		<result property="regDate" column="reg_date"/>
		<result property="img" column="img"/>
		<result property ="content" column="content"/>
		<result property ="readCount" column="read_count"/>
		
	</resultMap>
</mapper>